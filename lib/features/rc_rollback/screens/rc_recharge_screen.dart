import 'package:flutter/material.dart';

class  RcRechargeScreen extends StatelessWidget {
  const RcRechargeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Recharge Screen'),
      ),
    );
  }
}