import 'package:flutter/material.dart';

class RcRollbackScreen extends StatelessWidget {
  const RcRollbackScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Rollback Screen.')),
    );
  }
}